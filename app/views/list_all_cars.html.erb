<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Lista de carros</title>
</head>
<body>
  <div class="container">
    <h1>Lista de veículos</h1>
      <div class="row">
        <div class="col-lg-12 form-group">
          <table style="width:100%">
            <% if @cars.size > 0 %>
            <% @cars.each do |element| %>
            <tr>
              <th>ID</th>
              <th>Placa</th>
              <th>Marca</th>
              <th>Modelo</th>
              <th>Ano</th>
              <th>Km</th>
              <th>Tipo de Combustível</th>
              <th>Capacidade</th>
              <th>Atualizar</th>
              <th>Add. Abastecimento</th>
              <th>Remover</th>
            </tr>
            <tr>
              <td><%= element.id %></td>
              <td><%= element.plate %></td>
              <td><%= element.manufacturer %></td>
              <td><%= element.model %></td>
              <td><%= element.year %></td>
              <td><%= element.odometer %></td>
              <td><%= element.fuel_type %></td>
              <td><%= element.fuel_capacity %></td>
              <td><%= link_to root_path, class: "" do %><i class="glyphicon glyphicon-edit"></i><% end %></td>
              <td>
                <form action="/supplies/get/<%=element.id%>" method="get">
                  <button type="submit" class="btn-glyphicon">
                    <i class="glyphicon glyphicon-scale"></i>
                  </button>
                </form>

              </td>
              <td><%= link_to root_path, class: "" do %><i class="glyphicon glyphicon-remove"></i><% end %></td>
              </tr>
              <% end %>

              <% else %>
              Não existem veículos cadastrados
              <% end %>

          </table>
        </div>
      </div>
  </div>
</body>
</html>
